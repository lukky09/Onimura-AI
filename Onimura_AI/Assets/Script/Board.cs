using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Board : MonoBehaviour 
{

    GameObject[,] objarr;

    // Start is called before the first frame update
    void Start()
    {
        objarr = new GameObject[5,5];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
